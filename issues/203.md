```
$ donazopy
NAME
    donazopy

SYNOPSIS
    donazopy COMMAND

COMMANDS
    COMMAND is one of the following:

     provider

     provider_export_zone

     provider_import_zone

     provider_nameservers

     provider_records

     provider_status

     providers

     validate_zone

     version

     zone_diff

     zone_dump

     zone_normalize
```

1. Produce an extensive ProtoDocs+MaterialX documentation for the project (sources in src_docs, markdowns in src_docs/md, final compiles in docs/ )

2. Add CLI commands for the following:

- list providers
- show provider status
- export zone (with options to skip `NS` records, skip specified comma-separated types of records) 
- import zone
- copy zone from one provider to another (with options to skip `NS` records, skip specified comma-separated types of records, and also with an option to remove the pre-existing records from the target provider) 
- assign nameservers
- diff zones 

1. Use the following notation across ALL CLI commands for the domain: 
2. `[provider/][domain][:record_type][:host_name][:value]`

- `domain` = given domain on the provider that currently manages it, any record type, any host name, any value
- `provider/domain` or `provider/domain:*` or `provider/domain:*:*:*` = given domain on the given provider, any record type, any host name, any value
- `provider/*` = all domains on the given provider (any record type, any host name, any value etc.)

Simplify the structure of the CLI commands taking the above notation into account. 

